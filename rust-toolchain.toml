# Canonical toolchain for Greentic WASM repos.
# Source: greenticai/.github/toolchain/wasm/rust-toolchain.toml
# Sync: ./sync-toolchain.sh (from workspace root)
# Do not edit per-repo.

[toolchain]
channel = "1.95.0"
components = ["clippy", "rustfmt"]
targets = ["wasm32-wasip2"]
