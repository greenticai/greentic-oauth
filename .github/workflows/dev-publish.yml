# Auto-generated by sync-dev-publish.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Dev Publish

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  dev-publish:
    uses: greenticai/.github/.github/workflows/dev-publish.yml@main
    with:
      # greentic-oauth-broker intentionally omitted: legacy ci.yml excludes it from
      #   build/clippy/test and the crate has never been published from CI (legacy
      #   publish.yml ran in dry-run mode). Has pre-existing test failure in
      #   tests/http_flow.rs::start_to_callback_happy_path. No other crate depends on it.
      crates: "greentic-oauth-core greentic-oauth-host greentic-oauth-sdk greentic-oauth-client"
      # oidc-ingress / oidc-provider-runtime: cdylib WASM components that cannot build natively.
      # greentic-oauth-broker: excluded here too so build/clippy/test skip it (matches legacy ci.yml).
      exclude-crates: "oidc-ingress oidc-provider-runtime greentic-oauth-broker"
      wasm-target: true
    secrets: inherit
